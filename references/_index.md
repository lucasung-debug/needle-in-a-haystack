# references/_index.md — catalog (read this, then load exactly one file)

> **Load when:** you're unsure which reference a phase needs. Read this index, then open the **one** file that
> matches — don't preload all six (if the scaffolding becomes the haystack, the needle gets missed). `SKILL.md` is
> the router; `research.md` is always first.

| File | What it is | Load when (the *one* condition) |
|------|------------|---------------------------------|
| `research.md` | **The system directive (정전).** Runs the full cycle (FRAME→…→REPORT) and carries LAW 0, the 4 layers, the 6 axes, BYOK/cost, modes, and the compliance block. | **Always, first** — every run. |
| `philosophy.md` | **The single root — Peirce's Abduction Loop** and the epistemic commitments everything derives from. | FRAME or FALSIFY needs the *why* behind a gate. |
| `paradigm.md` | The **directional lens** (10-paradigm landscape, to *branch* the question's direction — not declare a worldview), the **operating standard** (4 layers / 13 enforceable directives; Heuer ACH, ICD 203), and the philosophy-of-science backbone that *disciplines* the one loop. 34 citations. | FRAME (set direction) · FALSIFY (apply the discipline). |
| `dialogue.md` | The **staged framing dialogue** (constructive Cartesian doubt + Socratic elenchus) that turns a request into a real question + a negative condition. | Start of FRAME (plan/full mode); compressed in lite. |
| `methodology.md` | **Domain-general research design** (social/medical/engineering/natural-science/humanities): question taxonomy, design menu, validity/bias/ethics — the 조사계획서 generator. | DESIGN, when producing a research plan/protocol. |
| `sources.md` | **Research-data source catalog + BYOK discovery** (scholarly/clinical/patent/dataset APIs, mostly free/keyless) and the NICE-TO-HAVE recommendation. | ROUTE & COST, mapping each evidence need → source. |

Not in `references/` (siblings of `SKILL.md`):
- `../scripts/compliance_check.py` — the output gate (exit non-zero = don't ship). Run at REPORT.
- `../eval/` — the frozen judge (fixtures + `runner.py`); run after touching the validator.
- `../templates/` — fill-in skeletons: `proposal` (조사계획서) · `report` · `framed-question` · `compliance-block` · `askuserquestion.snippet.json`.
